trigger:
  branches:
    include:
      - main

stages:
  - stage: Build
    jobs:
      - job: BuildChangedServices
        steps:
          - script: echo "Detect changed services and build staging images here."

