trigger: none

stages:
  - stage: Approval
    jobs:
      - deployment: DeployProduction
        environment: production
        strategy:
          runOnce:
            deploy:
              steps:
                - script: echo "Deploy approved service images to AKS production."
